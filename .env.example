# Supabase Configuration - Example Environment Variables
# Create a .env.local file with your actual values based on this template
# NOTE: DO NOT commit your actual .env.local file to version control!

# Supabase API Settings
# ----------------------------------------
# Supabase URL (endpoint for API requests)
SUPABASE_URL=https://your-project-id.supabase.co

# Supabase Anonymous Key (for public client-side access)
# This key can be used in browser environments as it has limited permissions
SUPABASE_ANON_KEY=your-anon-key-goes-here

# Supabase Service Key (for secure server-side access only)
# ⚠️ NEVER expose this key in client-side code or public repositories ⚠️
SUPABASE_SERVICE_KEY=your-service-key-goes-here

# JWT Secret for token verification
SUPABASE_JWT_SECRET=your-jwt-secret-goes-here

# Storage URL for file uploads and downloads
SUPABASE_STORAGE_URL=https://your-project-id.supabase.co/storage/v1

# Database Connection Settings
# ----------------------------------------
# Full connection string (includes all connection details)
DATABASE_URL=postgresql://postgres:your-password@db.your-project-id.supabase.co:5432/postgres

# Individual connection parameters
DB_HOST=db.your-project-id.supabase.co
DB_PORT=5432
DB_NAME=postgres
DB_USER=postgres
DB_PASSWORD=your-database-password
